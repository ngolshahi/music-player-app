import { RepeatMode } from "react-native-track-player"

const repeatOrder = [
    RepeatMode.Off,
    RepeatMode.Track,
    RepeatMode.Queue,
] as const

export const PlayerRepeatToggle = () => {
    
}